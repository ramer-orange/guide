# CDK Infrastructure Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `infra/` に TypeScript CDK プロジェクトを作成し、EC2・S3・Security Group・IAM・Elastic IP を `cdk deploy` 一発で構築できるようにする。

**Architecture:** デフォルトVPCにEC2（t4g.small / Ubuntu 24.04 LTS ARM）を配置し、Security GroupでSSH/HTTP/HTTPSのみ開放する。EC2 IAM RoleでS3への最小権限アクセスを付与し、Elastic IPを自動アタッチする。EC2 Key PairはCDKで作成してSSM Parameter Storeに秘密鍵を保存する。User DataでDocker・Git・AWS CLIを自動インストールする。

**Tech Stack:** TypeScript, aws-cdk-lib v2, ts-node, jest + ts-jest

---

## File Structure

```
infra/
├── bin/
│   └── infra.ts           # CDK appエントリポイント
├── lib/
│   └── guide-stack.ts     # メインスタック（全リソース）
├── test/
│   └── guide-stack.test.ts # CDK assertionsテスト
├── cdk.json               # CDK設定 + context(allowedSshCidr)
├── package.json
├── tsconfig.json
└── jest.config.js
```

---

### Task 1: CDKプロジェクト初期化

**Files:**
- Create: `infra/package.json`
- Create: `infra/tsconfig.json`
- Create: `infra/jest.config.js`
- Create: `infra/cdk.json`

- [ ] **Step 1: infraディレクトリを作成する**

```bash
mkdir -p infra/bin infra/lib infra/test
```

- [ ] **Step 2: package.jsonを作成する**

`infra/package.json`:

```json
{
  "name": "guide-infra",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "cdk": "cdk"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "22.x",
    "aws-cdk": "2.x",
    "jest": "^29.7.0",
    "source-map-support": "^0.5.21",
    "ts-jest": "^29.2.0",
    "ts-node": "^10.9.2",
    "typescript": "~5.6.0"
  },
  "dependencies": {
    "aws-cdk-lib": "2.x",
    "constructs": "^10.0.0"
  }
}
```

- [ ] **Step 3: tsconfig.jsonを作成する**

`infra/tsconfig.json`:

```json
{
  "compilerOptions": {
    "target": "ES2020",
    "lib": ["es2020"],
    "module": "commonjs",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "outDir": "dist",
    "rootDir": "."
  },
  "exclude": ["node_modules", "cdk.out", "dist"]
}
```

- [ ] **Step 4: jest.config.jsを作成する**

`infra/jest.config.js`:

```js
module.exports = {
  testEnvironment: 'node',
  roots: ['<rootDir>/test'],
  transform: { '^.+\\.tsx?$': 'ts-jest' },
};
```

- [ ] **Step 5: cdk.jsonを作成する**

`infra/cdk.json`:

```json
{
  "app": "npx ts-node --prefer-ts-exts bin/infra.ts",
  "context": {
    "allowedSshCidr": "x.x.x.x/32",
    "@aws-cdk/aws-ec2:restrictDefaultSecurityGroup": true,
    "@aws-cdk/aws-iam:minimizePolicies": true
  }
}
```

- [ ] **Step 6: npm installを実行する**

```bash
cd infra && npm install
```

Expected: `node_modules/` が作成される。エラーなし。

- [ ] **Step 7: コミットする**

```bash
cd ..
git add infra/package.json infra/package-lock.json infra/tsconfig.json infra/jest.config.js infra/cdk.json
git commit -m "feat(infra): CDKプロジェクト初期化"
```

---

### Task 2: bin/infra.ts を作成する

**Files:**
- Create: `infra/bin/infra.ts`

- [ ] **Step 1: bin/infra.tsを作成する**

`infra/bin/infra.ts`:

```typescript
#!/usr/bin/env node
import 'source-map-support/register';
import * as cdk from 'aws-cdk-lib';
import { GuideStack } from '../lib/guide-stack';

const app = new cdk.App();
new GuideStack(app, 'GuideStack', {
  env: {
    account: process.env.CDK_DEFAULT_ACCOUNT,
    region: 'ap-northeast-1',
  },
});
```

- [ ] **Step 2: コミットする**

```bash
git add infra/bin/infra.ts
git commit -m "feat(infra): CDK appエントリポイント追加"
```

---

### Task 3: テストを書く（失敗することを確認する）

**Files:**
- Create: `infra/test/guide-stack.test.ts`

- [ ] **Step 1: テストファイルを作成する**

`infra/test/guide-stack.test.ts`:

```typescript
import * as cdk from 'aws-cdk-lib';
import { Match, Template } from 'aws-cdk-lib/assertions';
import { GuideStack } from '../lib/guide-stack';

function makeStack(allowedSshCidr = '1.2.3.4/32'): Template {
  const app = new cdk.App({ context: { allowedSshCidr } });
  const stack = new GuideStack(app, 'TestStack', {
    env: { account: '123456789012', region: 'ap-northeast-1' },
  });
  return Template.fromStack(stack);
}

test('S3バケットが2つ作成され、パブリックアクセスがブロックされている', () => {
  const template = makeStack();
  template.resourceCountIs('AWS::S3::Bucket', 2);
  template.allResourcesProperties('AWS::S3::Bucket', {
    PublicAccessBlockConfiguration: {
      BlockPublicAcls: true,
      BlockPublicPolicy: true,
      IgnorePublicAcls: true,
      RestrictPublicBuckets: true,
    },
  });
});

test('バックアップバケットに7日ライフサイクルルールがある', () => {
  const template = makeStack();
  template.hasResourceProperties('AWS::S3::Bucket', {
    BucketName: 'guide-production-backups',
    LifecycleConfiguration: {
      Rules: Match.arrayWith([
        Match.objectLike({ ExpirationInDays: 7, Status: 'Enabled' }),
      ]),
    },
  });
});

test('EC2インスタンスがt4g.smallである', () => {
  const template = makeStack();
  template.hasResourceProperties('AWS::EC2::Instance', {
    InstanceType: 't4g.small',
  });
});

test('Security GroupがSSH/HTTP/HTTPSを許可している', () => {
  const template = makeStack();
  template.hasResourceProperties('AWS::EC2::SecurityGroup', {
    SecurityGroupIngress: Match.arrayWith([
      Match.objectLike({ CidrIp: '1.2.3.4/32', FromPort: 22, ToPort: 22 }),
      Match.objectLike({ CidrIp: '0.0.0.0/0', FromPort: 80, ToPort: 80 }),
      Match.objectLike({ CidrIp: '0.0.0.0/0', FromPort: 443, ToPort: 443 }),
    ]),
  });
});

test('Elastic IPが作成されている', () => {
  const template = makeStack();
  template.resourceCountIs('AWS::EC2::EIP', 1);
});
```

- [ ] **Step 2: テストを実行して失敗することを確認する**

```bash
cd infra && npm test
```

Expected: FAIL — `Cannot find module '../lib/guide-stack'`

- [ ] **Step 3: コミットする**

```bash
git add infra/test/guide-stack.test.ts
git commit -m "test(infra): GuideStackのテスト追加"
```

---

### Task 4: GuideStack を実装する

**Files:**
- Create: `infra/lib/guide-stack.ts`

- [ ] **Step 1: guide-stack.tsを作成する**

`infra/lib/guide-stack.ts`:

```typescript
import * as cdk from 'aws-cdk-lib';
import * as ec2 from 'aws-cdk-lib/aws-ec2';
import * as iam from 'aws-cdk-lib/aws-iam';
import * as s3 from 'aws-cdk-lib/aws-s3';
import { Construct } from 'constructs';

export class GuideStack extends cdk.Stack {
  constructor(scope: Construct, id: string, props?: cdk.StackProps) {
    super(scope, id, props);

    const allowedSshCidr: string =
      this.node.tryGetContext('allowedSshCidr') ?? '0.0.0.0/0';

    // デフォルトVPC
    const vpc = ec2.Vpc.fromLookup(this, 'Vpc', { isDefault: true });

    // S3: 添付ファイル用
    const filesBucket = new s3.Bucket(this, 'FilesBucket', {
      bucketName: 'guide-production-files',
      blockPublicAccess: s3.BlockPublicAccess.BLOCK_ALL,
      removalPolicy: cdk.RemovalPolicy.RETAIN,
      enforceSSL: true,
    });

    // S3: バックアップ用（7日で自動削除）
    const backupsBucket = new s3.Bucket(this, 'BackupsBucket', {
      bucketName: 'guide-production-backups',
      blockPublicAccess: s3.BlockPublicAccess.BLOCK_ALL,
      removalPolicy: cdk.RemovalPolicy.RETAIN,
      enforceSSL: true,
      lifecycleRules: [
        {
          enabled: true,
          expiration: cdk.Duration.days(7),
        },
      ],
    });

    // IAM Role（EC2からS3へのアクセス権限）
    const role = new iam.Role(this, 'Ec2Role', {
      assumedBy: new iam.ServicePrincipal('ec2.amazonaws.com'),
      description: 'Guide app EC2 role',
    });
    filesBucket.grantReadWrite(role);
    backupsBucket.grantReadWrite(role);

    // Security Group
    const sg = new ec2.SecurityGroup(this, 'SecurityGroup', {
      vpc,
      description: 'Guide app: SSH/HTTP/HTTPS',
      allowAllOutbound: true,
    });
    sg.addIngressRule(
      ec2.Peer.ipv4(allowedSshCidr),
      ec2.Port.tcp(22),
      'SSH from allowed CIDR',
    );
    sg.addIngressRule(ec2.Peer.anyIpv4(), ec2.Port.tcp(80), 'HTTP');
    sg.addIngressRule(ec2.Peer.anyIpv4(), ec2.Port.tcp(443), 'HTTPS');

    // EC2 Key Pair（秘密鍵はSSM Parameter Storeに保存）
    const keyPair = new ec2.KeyPair(this, 'KeyPair', {
      keyPairName: 'guide-ec2-key',
    });

    // User Data（Docker, Docker Compose v2, Git, AWS CLI v2 を自動インストール）
    const userData = ec2.UserData.forLinux();
    userData.addCommands(
      'apt-get update -y',
      'apt-get install -y ca-certificates curl gnupg unzip git',
      // Docker
      'install -m 0755 -d /etc/apt/keyrings',
      'curl -fsSL https://download.docker.com/linux/ubuntu/gpg | gpg --dearmor -o /etc/apt/keyrings/docker.gpg',
      'chmod a+r /etc/apt/keyrings/docker.gpg',
      'echo "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/ubuntu $(. /etc/os-release && echo $VERSION_CODENAME) stable" | tee /etc/apt/sources.list.d/docker.list > /dev/null',
      'apt-get update -y',
      'apt-get install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin',
      'systemctl enable docker',
      'systemctl start docker',
      'usermod -aG docker ubuntu',
      // AWS CLI v2（ARM）
      'curl -s "https://awscli.amazonaws.com/awscli-exe-linux-aarch64.zip" -o "/tmp/awscliv2.zip"',
      'unzip -q /tmp/awscliv2.zip -d /tmp',
      '/tmp/aws/install',
      'rm -rf /tmp/awscliv2.zip /tmp/aws',
    );

    // EC2インスタンス（t4g.small, Ubuntu 24.04 LTS ARM, EBS gp3 20GB）
    const instance = new ec2.Instance(this, 'Instance', {
      vpc,
      instanceType: ec2.InstanceType.of(
        ec2.InstanceClass.T4G,
        ec2.InstanceSize.SMALL,
      ),
      machineImage: ec2.MachineImage.fromSsmParameter(
        '/aws/service/canonical/ubuntu/server/24.04/stable/current/arm64/hvm/ebs-gp3/ami-id',
      ),
      securityGroup: sg,
      keyPair,
      role,
      userData,
      blockDevices: [
        {
          deviceName: '/dev/sda1',
          volume: ec2.BlockDeviceVolume.ebs(20, {
            volumeType: ec2.EbsDeviceVolumeType.GP3,
            deleteOnTermination: true,
          }),
        },
      ],
    });

    // Elastic IP
    const eip = new ec2.CfnEIP(this, 'ElasticIp', {
      domain: 'vpc',
      instanceId: instance.instanceId,
    });

    // Outputs
    new cdk.CfnOutput(this, 'ElasticIpAddress', {
      value: eip.ref,
      description: 'DNSのAレコードにこのIPを設定する',
    });
    new cdk.CfnOutput(this, 'SshPrivateKeyParameter', {
      value: keyPair.privateKey.parameterName,
      description: 'SSH秘密鍵のSSMパラメータ名',
    });
    new cdk.CfnOutput(this, 'InstanceId', {
      value: instance.instanceId,
      description: 'EC2インスタンスID',
    });
  }
}
```

- [ ] **Step 2: テストを実行してパスすることを確認する**

```bash
cd infra && npm test
```

Expected: 5 tests passed

- [ ] **Step 3: コミットする**

```bash
git add infra/lib/guide-stack.ts
git commit -m "feat(infra): GuideStack実装（EC2/S3/SG/IAM/EIP）"
```

---

### Task 5: CDK Bootstrap と Deploy

- [ ] **Step 1: cdk.jsonのallowedSshCidrを自分のIPに更新する**

自分のIPアドレスを確認する：

```bash
curl -s ifconfig.me
```

`infra/cdk.json` の `allowedSshCidr` を実際のIPに更新する：

```json
{
  "context": {
    "allowedSshCidr": "（取得したIP）/32"
  }
}
```

- [ ] **Step 2: CDK Bootstrapを実行する（初回のみ）**

```bash
cd infra && npx cdk bootstrap aws://$(aws sts get-caller-identity --query Account --output text)/ap-northeast-1
```

Expected:
```
✅  Environment aws://949925037534/ap-northeast-1 bootstrapped.
```

- [ ] **Step 3: CDK synthでCloudFormationテンプレートを確認する**

```bash
cd infra && npx cdk synth
```

Expected: CloudFormationテンプレートがstdoutに出力される。エラーなし。

- [ ] **Step 4: CDK deployを実行する**

```bash
cd infra && npx cdk deploy
```

途中で確認が出たら `y` を入力する。

Expected:
```
✅  GuideStack

Outputs:
GuideStack.ElasticIpAddress = x.x.x.x
GuideStack.SshPrivateKeyParameter = /ec2/keypairs/...
GuideStack.InstanceId = i-xxxxxxxxxxxxxxxxx
```

- [ ] **Step 5: SSH秘密鍵をダウンロードしてSSH接続を確認する**

OutputsのSSMパラメータ名を使って秘密鍵を取得する：

```bash
aws ssm get-parameter \
  --name "（SshPrivateKeyParameterの値）" \
  --with-decryption \
  --query Parameter.Value \
  --output text > ~/.ssh/guide-ec2.pem

chmod 600 ~/.ssh/guide-ec2.pem
```

SSH接続を確認する：

```bash
ssh -i ~/.ssh/guide-ec2.pem ubuntu@（ElasticIpAddressの値）
```

Expected: `ubuntu@ip-xxx-xxx-xxx-xxx:~$` のようなプロンプト

- [ ] **Step 6: DockerとGitがインストールされているか確認する**

EC2上で実行：

```bash
docker --version
docker compose version
git --version
aws --version
```

Expected: 各コマンドのバージョンが表示される。

- [ ] **Step 7: コミットする**

```bash
exit  # EC2からローカルに戻る
cd /Users/kai/guide
git add infra/cdk.json
git commit -m "feat(infra): cdk deploy完了、SSH確認済み"
```
